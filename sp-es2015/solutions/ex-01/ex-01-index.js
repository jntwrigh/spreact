/*
 * In this exercise, work with your instructor to build a utility module
 * The module should export a function dlog() which logs to the
 * output div in the document.
 *
 * Have this module defined in dlog.js
 * Import the module (and dlog()) in this script, and then test it out.
 *
 */

import {dlog} from './dlog';

console.log( 'Testing dlog()...' );
dlog( 'Hello, world!' );
